#![allow(non_snake_case)]
#![allow(unused)]

use itertools::Itertools;
use proconio::{
    input,
    marker::{Chars, Usize1},
};
use std::collections::{BinaryHeap, HashMap, VecDeque};

fn main() {
    input! {
        N: usize,
        A: [usize; N],
    }
}
