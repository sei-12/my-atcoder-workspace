#![allow(non_snake_case)]
#![allow(unused)]

use proconio::{
    input,
    marker::{Chars, Usize1},
};
use std::{
    cmp::{max, min},
    collections::{BinaryHeap, HashMap, VecDeque},
};
use itertools::Itertools;

fn main() {
    input! {
        N: usize,
        A: [usize; N],
    }
}
